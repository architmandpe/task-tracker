# Backend API for multiuser task tracker

## Install
To reproduce the environment use *uv sync*

## Run
To start the server use *uv run uvicorn app.main:app --reload*

## Test
To test your endpoints use *uv run pytest*